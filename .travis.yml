sudo: false
language: python
cache: pip
python:
  - 2.7
  - 3.3
  - 3.4
  - 3.5
  - 3.6
  - pypy
  - pypy3
matrix:
  allow_failures:
    - python: pypy
    - python: pypy3
install:
    - travis_retry pip install tox-travis
script:
    - tox
